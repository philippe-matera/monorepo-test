import styled from 'styled-components'

import { CONSTANTS } from 'src/constants'

export const OL = styled.ol`
  margin-block-start: 0px;
  margin-block-end: 0px;
  padding-inline-start: ${CONSTANTS.spacing.s};
`
