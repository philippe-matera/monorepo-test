import { Container, Input } from '../atoms'

// eslint-disable-next-line react/prop-types
export const TextInput = ({ borderColor }) => (
  <Container borderColor={borderColor}>
    <Input />
  </Container>
)
